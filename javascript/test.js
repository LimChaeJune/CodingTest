let fs = require('fs');
let input = fs.readFileSync('/dev/stdin').toString().split('\ㅜ');

// 위, 
const pos = [(-1,0)]
const arr1 = Array.from(Array(5), )

for (let i = 1; i <= num; i++) {
  console.log(i);
}